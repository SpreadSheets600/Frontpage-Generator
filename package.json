{
  "scripts": {
    "cf:dev": "cd cloudflare-api && npx wrangler@latest dev",
    "cf:d1:init": "cd cloudflare-api && npx wrangler@latest d1 execute frontpage-db --local --file=./schema.sql",
    "frontend:dev": "python3 -m http.server 3000"
  },
  "dependencies": {
    "vercel": "^48.12.0"
  }
}
